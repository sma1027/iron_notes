<h2>My Notes</h2>

  <div class="col-xs-4">
    <ul>
      {{#each note in controller}}
      <li>{{#link-to 'note' note}}
        {{note.title}}
        {{/link-to}}</li>
        {{/each}}
      </ul>
  </div>
  <div class="col-xs-8">
    {{outlet}}
  </div>
